{
  "name": "aurbit-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client:dev": "yarn workspace client start"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
